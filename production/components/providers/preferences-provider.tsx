"use client";
import{createContext,useContext,useEffect,useState}from"react";
type Theme="dark"|"light";type Language="EN"|"TH";type Preferences={theme:Theme;language:Language;toggleTheme:()=>void;toggleLanguage:()=>void};const C=createContext<Preferences|null>(null);const THEME="tradoviaTheme",LANG="tradoviaLanguage";
export function PreferencesProvider({children}:{children:React.ReactNode}){const[theme,setTheme]=useState<Theme>("dark"),[language,setLanguage]=useState<Language>("EN"),[ready,setReady]=useState(false);useEffect(()=>{try{const t=localStorage.getItem(THEME),l=localStorage.getItem(LANG);if(t==="dark"||t==="light")setTheme(t);if(l==="EN"||l==="TH")setLanguage(l)}finally{setReady(true)}},[]);useEffect(()=>{document.documentElement.dataset.theme=theme;if(ready)localStorage.setItem(THEME,theme)},[theme,ready]);useEffect(()=>{document.documentElement.lang=language==="TH"?"th":"en";if(ready)localStorage.setItem(LANG,language)},[language,ready]);return <C.Provider value={{theme,language,toggleTheme:()=>setTheme(v=>v==="dark"?"light":"dark"),toggleLanguage:()=>setLanguage(v=>v==="EN"?"TH":"EN")}}>{children}</C.Provider>}
export function usePreferences(){const v=useContext(C);if(!v)throw new Error("usePreferences must be used inside PreferencesProvider");return v}
