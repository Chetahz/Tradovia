export function ToolPlaceholder({eyebrow,title,description,status}:{eyebrow:string;title:string;description:string;status:string}){return <section className="tv-card tool-panel"><div><p className="eyebrow">{eyebrow}</p><h2>{title}</h2><p>{description}</p></div><span className="data-state">{status}</span><div className="tool-empty"><strong>No unverified data is shown.</strong><p>The production integration will be connected only after its source, permissions and freshness behavior are verified.</p></div></section>}
